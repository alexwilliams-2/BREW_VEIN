<div class="container">

  <h1> 🛒 Cart</h1>
  <p> Your orders ↓ </p>
  <ul>
  <% @orders.each do |order| %>
    <li> 1x <%= order.beer.name %>
      <%= link_to "❌",
      order_path(order), style: 'text-decoration: none; font-size: 20px',
      data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
    </li>
    <% end %>
  </ul>
</li>

</div>
